#
# Конфигурационный файл WorldGuard для Minecraft
#
# Важные указания по редактированию:
# - НЕ ИСПОЛЬЗУЙТЕ ТАБУЛЯЦИЮ. Используйте только пробелы, иначе Bukkit выдаст ошибку.
#   В редакторах типа Notepad++ (рекомендуется для Windows) включите опцию
#   "заменять табы на пробелы" (Настройки > Параметры > Меню Языка).
# - Сохраняйте отступы. Они группируют параметры по категориям
#   (например, "enforce-single-session" относится к категории "protection").
# - Для проверки формата перед использованием вставьте содержимое на сайт
#   http://yaml-online-parser.appspot.com/ и убедитесь, что нет ошибок ("ERROR:").
# - Строки, начинающиеся с #, являются комментариями и игнорируются.
#
# ВНИМАНИЕ:
# Перед использованием проверьте таблицу совместимости WorldGuard,
# чтобы убедиться в работоспособности функций для вашей версии Bukkit.
#

# -- Этот раздел автоматически заменяется плагином в игре --